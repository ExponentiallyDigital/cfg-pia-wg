README
1. Add to the existing section "5. Using the app" on how to use device assignment, I will take screen shots and add them - leave PLACEHOLDERS for where humans will benefit from screenshots
2. In README we tell them to run getbins.sh but that is never deployed and now we don't need to talk about it as we offer to deploy the bins - remove?
3. Preparing the USB stick is overly detailed, it needs to be very simple and brief, the finding about what type of format to use should move to ARCHITECTURE.
4. Tip about auto fill needs to be updated as we have removed the default prefilled user name.
5. 5.2 note screens are different for Merlin and stock - Killswitch and inbound firewall only on Merlin. 
6. 5.3 add a very brief write up linked to a fuller description in TESTING: tell users how to create an app password for SMTP. Include different providers like outlook, Gmail and who else?
7. Explain that create GitHub issue is how to file bug reports, add to 13. Support after the line "This app is unsupported and may cause objects in mirrors to be closer than they appear. Batteries not included." Keep it very brief - max two short sentences, less is better.
8. Add a very brief provide a pointer in the best location in README to TESTING or ARCHITECTURE telling users how they can examine the shell script to check it is ok and not a security issue.
